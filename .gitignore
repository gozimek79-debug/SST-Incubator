__pycache__/
*.pyc
*.pyo
.env
storage/*.db
experiments/*
!experiments/.gitkeep

# v0.8.2: artefakty testowe/runtime (nie naleza do repo)
storage/birth_log.json
storage/test_dash_*.json
storage/snapshots/*.json
.pytest_cache/

# v0.7.2 Z4: artefakt pochodny Specyfikacji Kanonicznej PC-001, z zalozenia
# niekomitowany (SPECYFIKACJA_KANONICZNA_PC_001.md §0.4)
CANONICAL_PARAMETERS_REPORT.md

# v0.8.5: lokalna konfiguracja narzedzia (Claude Code), nie kod projektu
.claude/

# B4C-1 (03), zgloszenie CTO: surowe wyjscia runnera konfirmacyjnego PC-001
# (execution_package_v0_11/runners/pc_001_confirmatory_runner.py) - dry-run
# ORAZ docelowy przebieg konfirmacyjny (B6+). Decyzja CTO: dowodem
# wykonalnosci ma byc TEST i reczny manifest (reports/b4c01_dry_run_manifest.json),
# NIGDY surowe pliki wynikowe w repo - 207 przebiegow x 300 tickow x 3 pola
# (prediction/input/prediction_error) to juz dziesiatki MB z samego dry-run,
# docelowy przebieg (552 x 300 x 3) bylby wiekszy o rzad wielkosci. Bez tego
# wpisu "git add -A"/"git add ." wciaga je do commita bez ostrzezenia.
# Pliki v0.11 JUZ SLEDZONE pod tymi sciezkami (patrz git ls-files) zostaja w
# repo bez zmian - .gitignore nie odsledza tego, co juz jest dodane. To jest
# GRANICA CZASOWA (v0.11 commitowal je PRZED ta decyzja CTO), nie
# niekonsekwencja - nowe pliki pod tymi samymi katalogami nie beda juz
# dolaczane, stare zostaja jako zapis historyczny. (Celowo bez liczby -
# wpisana recznie do prozy nie niesie informacji, ktorej nie da "git
# ls-files", a jest dokladnie tym rodzajem literalu, ktory wielokrotnie byl
# tu zrodlem rozjazdu - B4C-1 (02) sam podal "dziesiec", obciete przez "| head".)
execution_package_v0_11/results/
execution_package_v0_11/logs/
